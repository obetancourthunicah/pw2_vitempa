<header>
    <section class="branding">
        <h1>{{title}}</h1>
    </section>
    <nav>
        <ul>
            {{#with menuOptions}}
                {{#each this}}
                <li><a href="{{url}}">{{label}}</a></li>
                {{/each}}
            {{/with}}
        </ul>
    </nav>
</header>